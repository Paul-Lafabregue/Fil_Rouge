{% extends 'base_admin.html.twig' %}

{% block title %}New Products{% endblock %}

{% block body %}
    
    <div class="bgn m-2 p-2 text-white d-flex justify-content-between">
        <h4 class="text-start align-items-center"  style="border-radius: 10px;">Ajouter un produit</h4>
	</div>
    
    <div class="bg-body m-2 p-4 shadow">

        {{ include('admin/products/_form.html.twig', {'button_label': 'Ajouter'}) }}

            <div class="col-md-8 text-center">
                <a href="{{ path('admin_products') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
