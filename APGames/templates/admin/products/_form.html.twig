{{ form_start(form) }}
    {# {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button> #}


    {# Reprend l'éléments ProductName #}
    <div class="row mb-4">
        <div class="form-group col-6">
            {{ form_row(form.pro_name) }}
        </div>

        {# Reprend l'éléments SupplierID #}
        <div class="form-group col-md-6">
            {{ form_row(form.pro_sup) }}
        </div>
    </div>
    <div class="row mb-4">
        {# Reprend l'éléments CategoryID #}
        <div class="form-group col-md-6">
            {{ form_row(form.pro_sub_cat) }}
        </div>
        {# Reprend l'éléments Référence #}
        <div class="form-group col-md-6">
            {{ form_row(form.pro_ref) }}
        </div>
    </div>
    <div class="row mb-4">    
        {# Reprend l'éléments TVA #}
        <div class="form-group col-md-4">
            {{ form_row(form.pro_vat) }}
        </div>
        <div class="form-group col-md-4">
            {{ form_row(form.pro_price) }}
        </div>
        {# Reprend l'éléments Quantité En Stock #}
        <div class="form-group col-md-4">
            {{ form_row(form.pro_stock) }}
        </div>
    </div>
    <div class="row mb-4">
        {# Reprend l'éléments Niveau D'alerte#}   
        <div class="form-group col-md-4">
            {{ form_row(form.pro_wording) }}
        </div>

        <div class="form-group col-md-4">
            {{ form_row(form.pro_description) }}
        </div>

        {# Reprend l'éléments picture #}
        <div class="form-group col-md-4">
            {{ form_row(form.pro_picture) }}
        </div>
    </div>
    <div class="row mb-4">
        <div class="form-group col-md-6">
            {{ form_row(form.pro_date_add) }}
        </div>

        <div class="form-group col-md-6">
            {{ form_row(form.pro_date_modif) }}
        </div>

    </div>

    <br>

    <div class="row row-cols-md-3">
            <div class="col text-center">
                <button class="btn btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
            </div>

{{ form_end(form) }}
