{% extends 'base.html.twig' %}

{% block title %}Subcategories{% endblock %}

{% block body %}
    <h1>Subcategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subcategory.id }}</td>
            </tr>
            <tr>
                <th>Sub_cat_name</th>
                <td>{{ subcategory.subCatName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subcategories_index') }}">back to list</a>

    <a href="{{ path('subcategories_edit', {'id': subcategory.id}) }}">edit</a>

    {{ include('subcategories/_delete_form.html.twig') }}
{% endblock %}
