{% extends 'base.html.twig' %}

{% block title %}New Products{% endblock %}

{% block body %}

    <div class="bgn mb-3 p-4">
        <h1 class="text-center text-warning">Ajouter un nouveau produit</h1>
    </div>
    
    <div class="bg-body p-4 border border-4 border-warning" style="border-radius: 1rem;">

        {{ include('products/_form.html.twig') }}

            <div class="col-md-8 text-center">
                <a href="{{ path('products_index') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>

{% endblock %}