{% extends 'base.html.twig' %}

{% block title %}Edit Products{% endblock %}

{% block body %}
    
    <div class="bgn mb-3 p-4">
        <h1 class="text-center text-warning">Modifier le produit</h1>
    </div>

    <div class="bg-body p-4 border border-4 border-warning rounded">

        {{ include('products/_form.html.twig', {'button_label': 'Modifier'}) }}

            <div class="col text-center">
                <a href="{{ path('products_index') }}" class="btn btn-secondary">Retour à la boutique</a>
            </div>

            <div class="col text-center">
                {{ include('products/_delete_form.html.twig') }}
            </div>
        </div>

    </div>
    
{% endblock %}